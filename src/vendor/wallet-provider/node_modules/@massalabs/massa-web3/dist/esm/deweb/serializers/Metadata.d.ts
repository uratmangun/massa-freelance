import { DeserializedResult, Serializable } from '../../basicElements';
export declare class Metadata implements Serializable<Metadata> {
    key: string;
    value: string;
    constructor(key?: string, value?: string);
    serialize(): Uint8Array;
    deserialize(data: Uint8Array, offset: number): DeserializedResult<Metadata>;
}
