"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.indexByOwnerKey = exports.indexByOwnerBaseKey = exports.addressToOwnerKey = exports.addressToOwnerBaseKey = void 0;
const basicElements_1 = require("../../basicElements");
/**
 * Returns the base key for the owner's address.
 * @param address - The website address
 * @returns The base key for the owner's address
 */
function addressToOwnerBaseKey(address) {
    const prefix = (0, basicElements_1.strToBytes)('\x01');
    const lengthBytes = basicElements_1.I32.toBytes(BigInt(address.length));
    const addressBytes = (0, basicElements_1.strToBytes)(address);
    return new Uint8Array([...prefix, ...lengthBytes, ...addressBytes]);
}
exports.addressToOwnerBaseKey = addressToOwnerBaseKey;
function addressToOwnerKey(address, owner) {
    return new Uint8Array([
        ...addressToOwnerBaseKey(address),
        ...(0, basicElements_1.strToBytes)(owner),
    ]);
}
exports.addressToOwnerKey = addressToOwnerKey;
/**
 * Returns the base key for the owner's list of websites.
 * @param owner - The owner's address
 * @returns The base key for the owner's list of websites
 */
function indexByOwnerBaseKey(owner) {
    const prefix = (0, basicElements_1.strToBytes)('\x00');
    const lengthBytes = basicElements_1.I32.toBytes(BigInt(owner.length));
    const ownerBytes = (0, basicElements_1.strToBytes)(owner);
    return new Uint8Array([...prefix, ...lengthBytes, ...ownerBytes]);
}
exports.indexByOwnerBaseKey = indexByOwnerBaseKey;
function indexByOwnerKey(owner, address) {
    return new Uint8Array([...indexByOwnerBaseKey(owner), ...(0, basicElements_1.strToBytes)(address)]);
}
exports.indexByOwnerKey = indexByOwnerKey;
